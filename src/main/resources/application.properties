# server config
server.display-name = MybatisDemo
server.port = 8000

# datasource config
mybatis.type-aliases-package = com.supermario.mixsuper.entity
spring.datasource.driverClassName = com.mysql.jdbc.Driver
spring.datasource.url = jdbc:mysql://localhost:3306/mixsuper?useUnicode=true&characterEncoding=utf-8
spring.datasource.username = root
spring.datasource.password = Passw0rd

# log config (without this, config logback.xml under resources)
logging.config = classpath:config/logback.xml


